trigger:
- main

pool: Default

variables:
  imageName: 'omardiablo/auredevops'
  tag: '$(Build.BuildId)'

steps:

- task: Docker@2
  inputs:
    containerRegistry: 'dockerhub-connection'
    repository: '$(imageName)'
    command: 'buildAndPush'
    Dockerfile: 'Dockerfile'   # correct Dockerfile path
    buildContext: ''            # folder with app code
    tags: |
      latest
      $(tag)